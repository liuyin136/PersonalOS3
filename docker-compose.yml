version: '3.8'

services:
  # ---- PostgreSQL with pgvector ----
  personalos-db:
    image: pgvector/pgvector:pg16
    container_name: personalos_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: personalos
    volumes:
      - personalos_db_data:/var/lib/postgresql/data
      - ./db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d personalos"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: always

  # ---- Redis (Arq broker + cache) ----
  redis:
    image: redis:7-alpine
    container_name: personalos_redis
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: always

  # ---- FastAPI backend ----
  api-server:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: personalos_api
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql+asyncpg://postgres:mysecretpassword@personalos-db:5432/personalos
      - MARKDOWN_DIR=/data/markdown
      - PIC_DIR=/data/pic
      - CORS_ORIGINS=http://localhost:3000
    volumes:
      - markdown_data:/data/markdown
      - pic_data:/data/pic
    depends_on:
      personalos-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"
    restart: always

  # ---- Arq background worker ----
  ai-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: personalos_worker
    command: arq worker.WorkerSettings
    tty: true
    stdin_open: true
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql+asyncpg://postgres:mysecretpassword@personalos-db:5432/personalos
      - MARKDOWN_DIR=/data/markdown
      - PIC_DIR=/data/pic
    volumes:
      - markdown_data:/data/markdown
      - pic_data:/data/pic
    depends_on:
      personalos-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 1G
    restart: always

  # ---- Next.js frontend (standalone) ----
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.standalone
    container_name: personalos_frontend
    ports:
      - "3000:3000"
    environment:
      - BACKEND_URL=http://api-server:8000
      - NEXT_PUBLIC_API_BASE_URL=
    depends_on:
      - api-server
    restart: always

volumes:
  personalos_db_data:
  redis_data:
  markdown_data:
  pic_data:
